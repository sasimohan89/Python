# Truthy Falsy
bool(5)
bool(0)
bool('')

# Ternary operator
is_friend = True
can_message = "message allowed" if is_friend else "message not allowed"
print(can_message)

# Short circuiting - Use 'or' to check conditions, for interpretor to be more efficient

print(not True)